apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-demo-app.fullname" . }}
  labels:
    {{- include "new-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-demo-app.selectorLabels" . | nindent 4 }}
