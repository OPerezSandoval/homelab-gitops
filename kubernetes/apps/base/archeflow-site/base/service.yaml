apiVersion: v1
kind: Service
metadata:
  name: archeflow-site
  labels:
    app: archeflow-site
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: archeflow-site
