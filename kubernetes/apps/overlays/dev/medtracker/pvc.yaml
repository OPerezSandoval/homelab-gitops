apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: medtracker
  namespace: medtracker-dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 1Gi
